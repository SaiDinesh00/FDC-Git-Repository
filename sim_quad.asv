clc, close all
clear

Initial_state = zeros(13,1);
n_init = eul2quat([0, 0, 0],"ZYX");
 = n_init;
X(:,1) = Initial_state;
dt = 1e-2;

for i = 1:1e3
    u(:, i) = ones(4,1)*1e3;
    X(:, i+1) = X(:, i) + dt .* dynamics2(X(:, i), u(:, i));
end


plot(X(3,:))